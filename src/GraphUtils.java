import java.util.HashSet;

public class GraphUtils {
    public void allSubgraphs(Graph G){
       /* HashSet subgraphsOfG = new HashSet<Graph>;
        HashSet powerNodes = Sets.powerSet(G.getNodes());
        for(nodeSet : powerNodes){
            HashSet edges = HashSet<Integer>();
            for(edge : G.getEdges()){
                return;
            }


        }*/
    }
}
